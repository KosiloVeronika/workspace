package com.epam.libraryManager.tags;

import java.io.IOException;
import java.util.Locale;

import javax.servlet.ServletException;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpSession;
import javax.servlet.jsp.JspException;
import javax.servlet.jsp.tagext.TagSupport;

import org.apache.log4j.Logger;

public class SwitchLocaleTag1 extends TagSupport {

	/**
	 * 
	 */
	private static final long serialVersionUID = 6984552542869211886L;
	private static final Logger LOG = Logger.getLogger(SwitchLocaleTag1.class);
	public SwitchLocaleTag1() {
				
	}

	@Override
	public int doStartTag() throws JspException {
	/*	HttpServletRequest request = (HttpServletRequest) pageContext.getRequest();
		HttpSession session = request.getSession(true);
		Locale locale = (Locale) session.getAttribute("locale");
		if(locale == null) {
			locale = Locale.getDefault();
		}
		String localeToString = locale.toString();
		LOG.debug("lol");
		try {
			LOG.debug("lol1");
			if("ru_RU".equals(localeToString)) {
				pageContext.include("/jspf/en_locale.jspf");
			} else {
				pageContext.include("/jspf/en_locale.jspf");
			}
		} catch(ServletException | IOException ex) {
			LOG.error(ex.getMessage());
		}*/
		return SKIP_BODY;
	}

}
